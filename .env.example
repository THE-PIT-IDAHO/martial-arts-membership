# Database connection string (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/dojostorm?schema=public"

# JWT secret for admin auth tokens — REQUIRED
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=""

# Environment (set to "production" in production)
NODE_ENV="development"

# Resend API key for email notifications (configure in Account > Preferences)
# RESEND_API_KEY=""

# App URL (used for magic links, password reset links, etc.)
# NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Stripe (configure in Account > Payments)
# STRIPE_SECRET_KEY=""
# STRIPE_WEBHOOK_SECRET=""

# Build mode: set to "true" for standalone Docker builds
# BUILD_STANDALONE="false"
